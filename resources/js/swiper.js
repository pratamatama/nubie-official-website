import Swiper from 'swiper';

var swiper = new Swiper('.swiper-container', {
    centeredSlides: true,
    centerInsufficientSlides: true,
    loop: false,
    slidesPerView: 'auto',
    roundLengths: true,
    spaceBetween: 15,
    initialSlide: 2,
});
